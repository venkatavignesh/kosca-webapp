POSTGRES_USER=postgres
POSTGRES_PASSWORD=changeme
POSTGRES_DB=kosca_ar

# Use docker-compose values for local dev
DATABASE_URL=postgresql://postgres:changeme@localhost:5434/kosca_ar?schema=public
REDIS_URL=redis://localhost:6379

PORT=3000

# Docker target handles dev/production builds (dev or prod)
DOCKER_TARGET=dev
NPM_SCRIPT=dev

AR_REPORT_PATH=
CUSTOMER_MASTER_PATH=

# Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
SESSION_SECRET=
